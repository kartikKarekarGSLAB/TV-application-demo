import styled from 'styled-components';

/**
 * Styled component added for the scrolling
 * row.
 */
const RibbonRowScrollingWrapper = styled.div`
    overflow-y: auto;
    overflow-x: hidden;
    flex-shrink: 1;
    flex-grow: 1;
`;

export { RibbonRowScrollingWrapper };
